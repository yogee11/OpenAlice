/**
 * Compat shim — Contract.aliceId declaration merge lives in
 * `@traderalice/uta-protocol`. Importing this file pulls in the
 * augmentation as a side-effect; barrel-importing the package directly
 * works the same way.
 */
import '@traderalice/uta-protocol'
