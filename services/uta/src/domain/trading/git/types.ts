/**
 * Compat shim — canonical git/operation types live in
 * `@traderalice/uta-protocol`. Re-export so existing relative imports
 * within `src/domain/trading/**` keep working until physical move.
 */
export * from '@traderalice/uta-protocol'
